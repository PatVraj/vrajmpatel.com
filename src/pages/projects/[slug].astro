---
import { getCollection, render } from "astro:content";
import WorkflowDemo from "../../components/WorkflowDemo.astro";
import ContactBlock from "../../components/ContactBlock.astro";
import CaseNavigation from "../../components/CaseNavigation.astro";
import { projectDemos } from "../../data/projectDemos";
import ProofGrid from "../../components/ProofGrid.astro";
import SystemPath from "../../components/SystemPath.astro";
import { isPublicProject, isCaseStudy, sortProjects } from "../../lib/projects";
import BaseLayout from "../../layouts/BaseLayout.astro";

export async function getStaticPaths() {
  const projects = (await getCollection("projects")).filter(isPublicProject);
  return projects.map((entry) => ({
    params: { slug: entry.id },
    props: { entry },
  }));
}

const { entry } = Astro.props;
const { Content } = await render(entry);
const projects = (await getCollection("projects")).filter(isPublicProject).filter(isCaseStudy).sort(sortProjects);
const nextProject = projects.length > 1 ? projects[(projects.findIndex(project => project.id === entry.id) + 1) % projects.length] : undefined;
const sections = [
  ...(entry.data.overview ? [{ id: "overview", label: "Overview" }] : []),
  ...(projectDemos[entry.id] ? [{ id: "walkthrough", label: "Example" }] : []),
  ...(entry.data.proof?.length ? [{ id: "results", label: "Results" }] : []),
  ...(entry.data.systemPath?.length ? [{ id: "architecture", label: "Architecture" }] : []),
  { id: "evidence", label: "Details & evidence" },
];
const hasHeaderResources = Boolean(
  (entry.data.privateRepo && !entry.data.status) ||
    (!entry.data.privateRepo && entry.data.github) ||
    entry.data.paper ||
    entry.data.link,
);
---

<BaseLayout
  title={`${entry.data.title} — Vraj Patel`}
  description={entry.data.summary}
  ogImage={`/og/${entry.id}.png`}
  ogImageAlt={`Project preview for ${entry.data.title}`}
  schemaType="WebPage"
>
  <article class="mx-auto max-w-3xl">
    <a
      href="/projects"
      class="inline-flex min-h-11 items-center text-sm underline decoration-slate-300 underline-offset-4 hover:text-blue-700 focus-visible:rounded-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-600 dark:decoration-slate-700 dark:hover:text-blue-400"
    >
      All projects
    </a>

    <header class="mt-6 border-b border-slate-200 pb-8 dark:border-slate-800 sm:mt-10 sm:pb-10">
      <p class="font-mono text-xs text-slate-500 dark:text-slate-400">
        {entry.data.domain}
      </p>
      <h1 class="mt-3 text-3xl font-semibold tracking-[-0.035em] text-slate-950 dark:text-white sm:text-5xl">
        {entry.data.title}
      </h1>
      {
        entry.data.summary && (
          <p class="mt-5 text-base leading-7 text-slate-600 dark:text-slate-400 sm:mt-6 sm:text-lg sm:leading-8">
            {entry.data.summary}
          </p>
        )
      }

      <div
        class:list={[
          "mt-5 flex min-h-11 flex-wrap items-center gap-x-5 gap-y-2 text-sm font-medium sm:mt-7",
          !hasHeaderResources && "hidden",
        ]}
      >
        {
          entry.data.privateRepo ? (
            !entry.data.status && (
              <span class="text-slate-500 dark:text-slate-400">Private project</span>
            )
          ) : entry.data.github && (
            <a
              href={entry.data.github}
              target="_blank"
              rel="noopener noreferrer"
              data-ph-event="outbound_link_clicked"
              data-ph-resource="github"
              data-ph-project={entry.id}
              class="inline-flex min-h-11 items-center underline decoration-slate-300 underline-offset-4 hover:text-blue-700 focus-visible:rounded-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-600 dark:decoration-slate-700 dark:hover:text-blue-400"
            >
              Source
            </a>
          )
        }
        {
          entry.data.paper && (
            <a
              href={entry.data.paper}
              target="_blank"
              rel="noopener noreferrer"
              data-ph-event="outbound_link_clicked"
              data-ph-resource={entry.data.paperResource ?? "paper"}
              data-ph-project={entry.id}
              class="inline-flex min-h-11 items-center underline decoration-slate-300 underline-offset-4 hover:text-blue-700 focus-visible:rounded-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-600 dark:decoration-slate-700 dark:hover:text-blue-400"
            >
              {entry.data.paperLabel ?? "Paper"}
            </a>
          )
        }
        {
          entry.data.link && (
            <a
              href={entry.data.link}
              target="_blank"
              rel="noopener noreferrer"
              data-ph-event="outbound_link_clicked"
              data-ph-resource="project_site"
              data-ph-project={entry.id}
              class="inline-flex min-h-11 items-center underline decoration-slate-300 underline-offset-4 hover:text-blue-700 focus-visible:rounded-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-600 dark:decoration-slate-700 dark:hover:text-blue-400"
            >
              Live site
            </a>
          )
        }
      </div>

      {
        (entry.data.contribution || entry.data.setting || entry.data.status) && (
          <dl class="mt-6 grid grid-cols-1 gap-x-5 gap-y-6 border-t border-slate-200 pt-6 sm:mt-8 sm:grid-cols-2 sm:gap-8 dark:border-slate-800">
            {entry.data.contribution && !entry.data.overview && (
              <div class="sm:col-span-1">
                <dt class="font-mono text-xs text-slate-500 dark:text-slate-400">
                  Contribution
                </dt>
                <dd class="mt-2 text-sm leading-6 text-slate-800 dark:text-slate-200">
                  {entry.data.contribution}
                </dd>
              </div>
            )}
            {entry.data.setting && (
              <div>
                <dt class="font-mono text-xs text-slate-500 dark:text-slate-400">
                  Setting
                </dt>
                <dd class="mt-2 text-sm leading-6 text-slate-800 dark:text-slate-200">
                  {entry.data.setting}
                </dd>
              </div>
            )}
            {entry.data.status && (
              <div>
                <dt class="font-mono text-xs text-slate-500 dark:text-slate-400">
                  Status
                </dt>
                <dd class="mt-2 text-sm leading-6 text-slate-800 dark:text-slate-200">
                  {entry.data.status}
                </dd>
              </div>
            )}
          </dl>
        )
      }
    </header>

    <CaseNavigation sections={sections} />
    {entry.data.overview && <section id="overview" class="case-overview" aria-labelledby="overview-heading" data-reveal>
      <p class="eyebrow">Overview</p>
      <h2 id="overview-heading" class="section-title">The problem</h2>
      <p>{entry.data.overview.problem}</p>
      <h3>My contribution</h3>
      <p>{entry.data.overview.built}</p>
    </section>}
    <WorkflowDemo project={entry.id} />
    {entry.data.proof?.length && <section id="results" class="page-section" aria-labelledby="project-evidence-heading">
      <p class="eyebrow">Measured and implemented</p>
      <h2 id="project-evidence-heading" class="section-title">Results, with their scope.</h2>
      <ProofGrid items={entry.data.proof} columns={3} class="mt-6" />
    </section>}
    {entry.data.systemPath?.length && <SystemPath steps={entry.data.systemPath} label={entry.data.systemPathLabel} heading={entry.data.systemPathHeading} description={entry.data.systemPathDescription} />}
    <section id="evidence" class="case-body" aria-label="Details and evidence"><Content /></section>

    {
      entry.data.tech && entry.data.tech.length > 0 && (
        <section class="border-t border-slate-200 pt-8 dark:border-slate-800" aria-labelledby="technologies-heading">
          <h2 id="technologies-heading" class="text-lg font-semibold text-slate-950 dark:text-white">
            Technologies
          </h2>
          <p class="mt-3 font-mono text-[0.8125rem] leading-6 text-slate-500 dark:text-slate-400">
            {entry.data.tech.join(" · ")}
          </p>
        </section>
      )
    }
    {nextProject && <a href={`/projects/${nextProject.id}`} class="related-project text-link" data-ph-event="project_opened" data-ph-project={nextProject.id} data-ph-placement="related_project"><span class="eyebrow">Continue exploring</span><strong>{nextProject.data.title}</strong></a>}
    <ContactBlock project={entry.data.title} compact />
  </article>
</BaseLayout>
